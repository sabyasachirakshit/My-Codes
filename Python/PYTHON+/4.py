# Accept Radius of circle from user and compute the area.

# Program-

import math
radius = float(input("Enter Radius: "))
area = math.pi*(radius**2)
print(f"Area of circle of radius {radius} is {area} squared units.")
